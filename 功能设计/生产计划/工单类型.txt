针对135：
一、现在的工单分为：例行工单RTN和非例行工单NRC，两种大类型。
其中，例行工单RTN页面上，可以出现的工单类型是：
1、维修项目（组包自动产生，不能手工创建）；2、EO（组包自动产生，不能手工创建）；4、MO/MCC（手工创建）；5、其他指令（手工创建）。
非例行工单NRC页面上，可以出现的工单类型是：
3、NRC（手工创建）；9、FLB（飞行记录本自动产生，不能手工创建）

二、例行工单（RTN），【来源任务号】：对应的工单类型为：1、维修项目和2、EO。  
三、非例行工单（NRC）的【来源工单】：例行工单，可选填，对应的工单类型为：1、维修项目；2、EO；4、MO/MCC；5、其他指令

针对145：
上述原则都满足，区别在于：除了非例行工单中9FLB类型外，其他所有的工单都可以手工创建。